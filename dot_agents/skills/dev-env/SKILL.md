---
name: dev-env
description: Start a repo's dev environment and hand over a working URL with login.
disable-model-invocation: true
---

Deliver: a URL the user can open and log into, reported together with credentials.
Some projects might be on tailscale, give the tailscale url in that case.
Verify url and credentials work.